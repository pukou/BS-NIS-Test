package com.bsoft.nis.service.nurserecord;

public class NumberCharHelper {
    public static boolean startWithCircle(String rawStr) {
        if (rawStr.startsWith("①")) {
            return true;
        }
        if (rawStr.startsWith("②")) {
            return true;
        }
        if (rawStr.startsWith("③")) {
            return true;
        }
        if (rawStr.startsWith("④")) {
            return true;
        }
        if (rawStr.startsWith("⑤")) {
            return true;
        }
        if (rawStr.startsWith("⑥")) {
            return true;
        }
        if (rawStr.startsWith("⑦")) {
            return true;
        }
        if (rawStr.startsWith("⑧")) {
            return true;
        }
        if (rawStr.startsWith("⑨")) {
            return true;
        }
        if (rawStr.startsWith("⑩")) {
            return true;
        }
        if (rawStr.startsWith("⑪")) {
            return true;
        }
        if (rawStr.startsWith("⑫")) {
            return true;
        }
        if (rawStr.startsWith("⑬")) {
            return true;
        }
        if (rawStr.startsWith("⑭")) {
            return true;
        }
        if (rawStr.startsWith("⑮")) {
            return true;
        }
        if (rawStr.startsWith("⑯")) {
            return true;
        }
        if (rawStr.startsWith("⑰")) {
            return true;
        }
        if (rawStr.startsWith("⑱")) {
            return true;
        }
        if (rawStr.startsWith("⑲")) {
            return true;
        }
        if (rawStr.startsWith("⑳")) {
            return true;
        }
        return false;
    }
}
